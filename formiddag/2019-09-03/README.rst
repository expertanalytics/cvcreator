To rebuilding slide deck:

* install dependencies using either ``pip install .`` or ``poetry install``.
* build using::

      $ hovercraft presentation.rst presentation

* Presentation can be found at ``presentation/index.html``.
